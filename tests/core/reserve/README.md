# Reserve Registry Unit Tests

Unit tests for the `reserve` registry core are kept inline in [tests.rs](file:///d:/TheAnhProject/UdonFi/contracts/core/reserve/tests.rs).

To run these tests, navigate to the `contracts/core/reserve/` folder and run:
```bash
cargo test
```
