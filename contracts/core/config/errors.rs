//! Protocol Configuration Engine errors.
//!
//! The config engine reuses the protocol-wide error enum instead of defining
//! a module-local error namespace.

pub use udonfi_shared::errors::LendingError;
